on:
  push:

jobs:
  first:
    steps:
      - uses: http://sigyl.com:3000/actions/checkout@v3.5.4
        with:
          token: ${{ secrets.BOT_TOKEN }}
      - uses: ./.gitea/actions/get-tea
        with:
          token: ${{ secrets.BOT_TOKEN }}
      - name: result
        run: |
          ../tea pr ls -o=simple
