on:
  push:

jobs:
  first:
    steps:
      - name: one
        run: echo done
  included:
    needs: first
    uses: ./.gitea/workflows/include.yaml
  second:
    needs: included
    steps:
      - name: one
        run: echo done
